Spring MVC 3.1 Demo
===================

This project contains a number of showcase style mini-samples that demonstate customizations and features available in Spring MVC 3.1. 

Deploy the application and see the various links available from the home page. To explore the code, navigate to `src/main/java/org/springframework/samples/mvc31` and see `package-info.java` in each sub-package.
